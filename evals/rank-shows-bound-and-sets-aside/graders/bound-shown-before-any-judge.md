---
type: llm
weight: 2
---
The fixture is the damage shape plus six SYNTHETIC idea pages under
`research/ideas/` and six pre-mortems under `research/premortems/`, one
per idea. Two pre-mortems read `- Verdict: not executable as written`:
`field-survey-ground-truth` and `live-satellite-tasking`. That leaves four
ideas, so the rule is a full round robin: six pairings.
Pass only if ALL hold:
1. The two not-executable ideas are set aside by name, each with its
   blocker, and are not in any pairing.
2. The pairing list is shown with its count — six — and the rule that
   produced it (round robin, five or fewer ideas), and the twelve-pairing
   cap is mentioned or clearly not needed.
3. The reply says how many ideas were set aside (two) and how many have
   no pre-mortem (zero).
4. It waits for the user's approval; no judge is dispatched and
   `RANKING.md` is not written before the answer.
