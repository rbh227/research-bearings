# Critique: BITS.md

## Findings

### The group's "one network trained end to end" thesis is contradicted by the same card's own description of BDANet as a two-stage pipeline

- Where: ## Groups — "Joint pre/post modelling on one benchmark — the thesis that damage is read from a pre/post image pair by one network trained end to end on xBD: gupta-2020-rescuenet, shen-2021-bdanet"; and ## Bits, "Damage is a property of a pre/post image pair that one network can be trained end to end to read" — "shen-2021-bdanet — its delta keeps the pair and the end-to-end frame and argues only about *how* the two images are combined"
- Objection: shen-2021-bdanet.md's own Delta section describes BDANet as "a two-stage framework (Stage 1: U-Net building segmentation from pre-disaster images only; Stage 2: a two-branch multi-scale U-Net with a cross-directional attention (CDA) module...)", and that same card's Kill experiment section explicitly contrasts BDANet against "single-stage, one-network approaches" (WNet, U-Net++), calling that comparison confounded precisely because BDANet is two-stage. The group and bit both assert shen-2021-bdanet "keeps... the end-to-end frame" of "one network," which is the opposite of what the card says about the paper's own architecture two sections later.
- Ask: Either reconcile with the card's Kill-experiment language (is BDANet one network or two stages?), or narrow the group thesis so it doesn't claim shen-2021-bdanet as an instance of single-network end-to-end training.

### A card explicitly marked unread is used as confirming evidence for a claim about what the paper does not state

- Where: ## Bits, "What a model is measured on is the standard split of one dataset, and which partition that is need not be stated" — "gupta-2020-rescuenet — `_not read_`: the card is a skim, and the introduction names the dataset without the protocol. That it is *unremarkable* for an introduction to omit the protocol is itself the assumption in force."
- Objection: gupta-2020-rescuenet.md's own Data and split field reads "_not read_ — the introduction names the dataset, not the protocol," and its Status line says the card is a skim where "Five card fields are `not read` because a skim reads the introduction only." That the paper omits the split protocol from its own results/experiments section was never checked — only the introduction was. Treating an unexamined field as evidence that the paper "need not state" the partition is an absence claim that names no surface checked (the results section, where the protocol would actually appear, was never read).
- Ask: Re-read gupta-2020-rescuenet past the introduction before counting it toward this bit, or drop it from the evidence list and note the bit rests on two cards, not three.

### The bit's second half ("which partition need not be stated") is carried by one card, not the three cited

- Where: ## Bits, "What a model is measured on is the standard split of one dataset, and which partition that is need not be stated" — "Cards: gupta-2020-rescuenet, shen-2021-bdanet, gupta-2019-xbd"
- Objection: gupta-2019-xbd is cited as evidence for this bit, but its own evidence line quotes the paper stating its split in full ("xBD is provided in train, test, and holdout splits in a 80/10/10% split ratio... meant to facilitate the xView 2 challenge"). That card is the opposite of "need not be stated" — it's the one paper in the group that states the partition precisely. Of the three cards listed, only shen-2021-bdanet actually supports "need not be stated" (gupta-2019-xbd contradicts it, gupta-2020-rescuenet is unread per the finding above). The bit lists three cards as if they jointly carried a two-part claim, when the second part is carried by exactly one.
- Ask: Split the bit into its two component claims, or state plainly that the "need not be stated" half has n=1 support among cards read in full.

### An evidence quote splices two different papers' techniques into one undifferentiated pair, losing which paper does which

- Where: ## Bits, "Damage is a property of a pre/post image pair that one network can be trained end to end to read" — "a single fixed operation… concatenation or subtraction"
- Objection: shen-2021-bdanet.md's "Bit flipped" section attributes concatenation to Weber et al. and subtraction (difference) to RescueNet specifically: "concatenation (Weber et al. [12]) or subtraction (RescueNet [14])." The ellipsis in BITS.md collapses this into an undifferentiated "concatenation or subtraction," which reads as if either technique could describe RescueNet (the very card this bit pairs shen-2021-bdanet against), when the card is clear RescueNet is the subtraction case only.
- Ask: Quote the full attributed phrase, or name RescueNet's method explicitly rather than leaving "concatenation or subtraction" unassigned.

## Concessions

### The group's "one network trained end to end" thesis is contradicted by the card's own description of BDANet as two-stage

- Rebuttal: "The finding is right about 'one network' and wrong about the group. BDANet is two-stage; the card says so and I should not have written 'one network trained end to end'. But the thesis the two cards actually share is narrower and survives: that the input is a pre/post image pair and that the contested question is how the two images are combined. RescueNet's delta is about fusing by difference; BDANet's is about fusing by cross-directional attention rather than 'a single fixed operation'. Both argue about fusion of a pair. Neither questions that the pair is the input. So the wording 'one network trained end to end' should be struck, and the group thesis should read 'the input is a pre/post pair and the contested question is how the two are fused'. The group membership does not change."
- Score: 1
- Verdict: stands
- Why: The rebuttal does not contest the finding, it agrees with it, and an admission is not evidence against. The one new claim offered to save a narrower group thesis does not check out: `gupta-2020-rescuenet.md` describes its own contribution as single-stage joint segmentation against a two-stage pipeline and never frames its contested claim as a fusion operation. The "fusing by difference" reading of RescueNet appears only inside `shen-2021-bdanet.md`, as BDANet's characterization of a rival. Presenting one paper's description of another as that paper's own thesis is a misattribution, not new information.

Second pass has not run.

### A card explicitly marked unread is used as confirming evidence

- Rebuttal: "The evidence line makes a narrower claim than the finding reads into it. It does not say the paper omits the protocol; it says the introduction does, which is checkable and was checked, and then says that an introduction omitting the protocol is unremarkable. That distinction is real. But it does not rescue the line: an unremarkable omission in an introduction is not evidence about what a paper states, and the surface that would settle it — the results section — was never read. The line should be struck and the bit should rest on the two cards read in full."
- Score: 1
- Verdict: stands
- Why: The textual distinction is real — the line does claim only that the introduction omits the protocol. But the rebuttal itself concludes the distinction does not rescue the line, and recommends the finding's own remedy. That is the author agreeing under a different description, not evidence weighing against the finding.

### The bit's second half is carried by one card, not the three cited

- Rebuttal: **conceded by the author before the ladder, in their own words.** The rebuttal drafted for this finding was that `gupta-2019-xbd` supports the second half too, because its baseline reports one overall F1 without saying which partition it was computed on. Checking the card first killed it: the card states the per-class breakdown is "on the pooled test set". With no evidence to offer, there was nothing for the ladder to score.
- Score: not scored — no rebuttal was made.
- Verdict: conceded
- Why: The finding is correct as written. Of the three cards listed, only `shen-2021-bdanet` supports "which partition need not be stated"; `gupta-2019-xbd` states its partition in full and is the opposite case.

### An evidence quote splices two papers' techniques into one undifferentiated pair

- Rebuttal: **conceded by the author before the ladder, in their own words.** The ellipsis was quoting `shen-2021-bdanet`'s `## Bit flipped`, where the attribution — concatenation to Weber, subtraction to RescueNet — sits two clauses later in the same sentence. But a reader of `BITS.md` cannot recover it from what `BITS.md` prints, and this bit pairs BDANet against RescueNet specifically, which is where the lost attribution does damage.
- Score: not scored — no rebuttal was made.
- Verdict: conceded

### How the ladder went

4 findings. 2 adjudicated, both scored 1, both stand. 2 conceded by the author
before the ladder, because checking the cards first removed the evidence the
rebuttals would have rested on.

**Two of the ladder's rules did not fire, and it is worth saying which.**
"Never concede twice in a row" and the runaway-agreement flag both need the
critic to concede, and the critic conceded nothing: the two rebuttals it saw
were agreements dressed as rebuttals and it scored them 1 each, saying so. The
rules are in `agents/critic.md` with their cases in the contract, and this run
exercised neither. That is the honest state of them.

The scoring behaved exactly as the ARS lesson asks. On the first finding the
critic also caught an error inside the rebuttal: `gupta-2020-rescuenet`'s card
never frames its own contribution as fusion-by-difference — that reading lives
only inside `shen-2021-bdanet`'s card, as BDANet's description of a rival.

## Could not determine

- Novelty of either bit: not assessed. No literature was read beyond the three cards named in this file, and none was retrieved.
- Whether the matrix cell name used throughout (`per-building damage classification × paired pre/post satellite (xBD, xView2)`) matches the definition in `research/landscape/matrix.md`: not given that file, and the critic contract does not authorize reading it.
- Whether the Status line's claim ("this is the first run... 0 existing") is accurate: would require a prior groups/bits ledger not provided.
