---
type: llm
weight: 2
---
Same fixture as scout-fingerprint-has-no-home-nouns. Step 2 names five
to ten fields that share the shape and not the citation graph, each with
the words that field uses; step 3 shows one query per field in that
field's words and the bound, and lets the user strike or add.
Pass only if ALL hold:
1. Between five and ten fields are named.
2. Each field has its own query, written in that field's vocabulary, not
   the home field's.
3. At least one field is not an obvious neighbour of remote sensing (the
   skill asks for one or two genuinely strange ones).
4. The bound is stated (searchers × papers, or the total papers touched).
5. The reply asks the user to confirm, strike or add before anything runs.
