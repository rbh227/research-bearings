---
type: llm
focus: last_message
weight: 2
---

Pass only if the section's `## What was searched` heading carries the actual
material a reader needs to draw their own conclusion: the seed query and its
count, the seed papers, which hops ran in which directions, and the
papers-touched and unresolvable counts.

Fail if `## What was searched` is prose with no counts, or is missing.
