---
type: llm
weight: 2
---
The fixture has a question and a landscape and nothing else: no
`BITS.md`, no analog pages, no `IDEAS.md`, no personas. The skill's
precondition says missing seed kinds are reported, not fatal; the matrix's
contradictions are the one seed source present.
Pass only if ALL hold:
1. The seed inventory names which seed kinds were found (contradiction,
   from the matrix) and which were absent (bit, analog, persona, and the
   log), rather than refusing outright.
2. Every seed it lists cites the file it came from.
3. If the index cannot be reached in this run, the reply says so and does
   not promote any candidate to a page — a candidate with no row count is
   not an idea here.
