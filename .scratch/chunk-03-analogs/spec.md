# Chunk 3: `/scout`, the breadth tool

The spec is `docs/design/chunk-03-analogs.md`, kept with the other chunk specs.
Tickets are in `issues/`, numbered in build order. Ticket 07 is the cleanup and
is last on purpose.
