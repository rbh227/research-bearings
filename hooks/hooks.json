{
  "description": "research-bearings scope guard: agents shipped by this plugin may only write under research/ and may only run the plugin's own retrieval script.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write|Edit|Bash",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}\"/hooks/guard.py",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
