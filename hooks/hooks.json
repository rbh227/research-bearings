{
  "description": "research-bearings guard: agents shipped by this plugin may only write under research/, may only run the retrieval script, and only searcher may WebSearch; no agent may WebFetch.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write|Edit|Bash|WebSearch|WebFetch",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}\"/hooks/guard.py",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
