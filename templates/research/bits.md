# Bits: <the question>

<!-- Written by /research-bearings:bits, at research/BITS.md. No agents.
     Four headings, fixed.

     Ré: every cluster of papers shares an implicit assumption, and a
     contribution flips it. This file is the list of assumptions the field is
     standing on, each traceable to the cards that share it.

     The groups are recorded here and REUSED on later runs. /ideas reads this
     file, and a file that reshuffles its groups every run is a file nothing
     downstream can build on. -->

## Groups

<!-- One line per thesis group, with the cards in it. Recorded on the first
     run and kept; a later run assigns new cards to existing groups and adds a
     group only for cards that fit none.

     - <group name — the shared thesis, in one clause>: <slug>, <slug> (since <date>)
     - <group name>: <slug> (new 2026-09-20)

     A group is never dissolved by a run. If a group's cards stop sharing a
     thesis, that is a note for a human, written under ## Status. -->

## Bits

<!-- One ### per group holding two or more cards, in the order the groups are
     listed.

     ### <the assumption, as one sentence stating what the group takes as given>
     - Kind: method | evaluation | dataset
     - Group: <group name>
     - Cards: <slug>, <slug>
     - Cells: <formulation × regime>, <formulation × regime>  (from the matrix;
       `unplaced` for cards with no cell)
     - Evidence: <one line per card — the clause in its ## Delta or
       ## Data and split that shows the assumption in force>
     - Flipped by: <slug>, if any card's ## Bit flipped already breaks it, or
       `nothing in these cards`

     Evaluation and dataset bits matter as much as method bits. "Everyone
     reports on the standard split" is an assumption, and it is the one that
     /audit has the taxonomy for. -->

## Too thin to name a bit

<!-- One line per group holding a single card: the group, the card, and what
     the assumption would be if a second card supported it. A bit from one
     paper is that paper's design choice, not the field's assumption. -->

## Status

<!-- Date. Cards read, groups (existing / new this run), bits written, groups
     too thin. Cards that fit no existing group and started one, named. Any
     group whose cards no longer look like one thesis, named for a human to
     split — this file never splits a group by itself. -->
