# Critique: <the file critiqued>

<!-- Written by the critic agent, at research/critiques/<file-slug>-<date>.md.
     One critiqued file, one run, one file. Three headings, fixed.

     The critic runs in a fresh context and never sees the reasoning that
     produced the file it attacks. That ignorance is the mechanism: a
     generator that judges its own work concedes to itself.

     This file never edits the file it judges. -->

## Findings

<!-- One block per finding, worst first.

     ### <the claim under attack, in one line>
     - Where: <## Heading> — "<the passage, quoted>"
     - Objection: <what is wrong with it. One or two sentences.>
     - Ask: <what would fix it, or what evidence would settle it>

     Every finding quotes the passage it attacks. A finding with no quote is
     an impression, and an impression cannot be rebutted or scored. -->

## Concessions

<!-- Written on the second pass, one block per finding the author rebutted.

     ### <the finding, restated in one line>
     - Rebuttal: "<the author's rebuttal, verbatim>"
     - Score: <1-5>
     - Verdict: stands | conceded
     - Why: <one or two sentences on what the score turns on>

     Then, if the concessions so far pass half the findings:
     FLAG FOR HUMAN — conceded on N of M findings.

     Score the rebuttal on whether it is evidence, not on whether it is
     persuasive. Concede only at 4 or above. Never concede twice in a row. -->

## Could not determine

<!-- What could not be assessed, and why. Novelty always lands here: the
     critic has read no literature and has no retrieval, so it cannot say
     whether something is new. Anything needing a file that was not provided
     lands here too, named. -->
