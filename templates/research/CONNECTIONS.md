# Connections

<!-- Written by /research-bearings:setup from `snowball.py status`. One line
     per source with its state and the date it was probed. Every searching
     skill reads this first and adapts: a source marked connected-no-key is
     used at its unkeyed rate, a source marked not connected is skipped and
     the skip is stamped in the output. Missing keys are never an error.
     Re-run `snowball.py status --md` to refresh; the lines go stale the
     moment a key changes, which is why each carries a date. -->

## Sources

<!-- Pasted from `status --md`, one line per source:
     - <source>: connected | connected-no-key | not connected (YYYY-MM-DD) — set <ENV> — <note>
-->

## Keys that would help most

<!-- The `suggest` block from status: at most three, in order, each with the
     env var, what it changes, and where to get it. Copied, not composed. If
     status suggests nothing, write "none missing". Setup names these once
     and stops; it does not ask you to go and get them. docs/APIS.md in the
     plugin has the rest. -->
