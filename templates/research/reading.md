# Reading: <title>

<!-- Written by the reader agent, which has been given the whole paper and
     never the prediction. At research/papers/notes/<slug>.reading.md.

     This note holds every card field the paper supports; the scorer copies
     them onto the card and adds only the score and the non-obvious line. The
     reader never sees what was predicted, so it cannot write toward it.
     Headings mirror the card, so the copy is mechanical. -->

## Identity

<!-- Authors, year, venue, ids, and the code link with whether the paper says
     it runs. From the paper and the fetch record, not from memory. -->

## Delta

<!-- Compared to <nearest prior work>, this changes <X> and gets <Y>.

     Or `_cannot be written_` and what is missing. Do not soften a paper into
     a delta it does not claim. -->

## Bit flipped

<!-- The shared assumption this paper breaks, or `_none_`. -->

## Not compared against

<!-- Baselines a reader would expect and did not find, one per line. -->

## Kill experiment

<!-- The experiment that would have falsified the central claim, and whether
     they ran it. -->

## Data and split

<!-- Dataset, split protocol in the paper's words, seeds and variance, metric.
     Quote the split protocol where it is stated ambiguously: /audit reads
     this and a paraphrase loses what it needs. -->

## Reproduction

<!-- Status, evidence, and whether it is reimplementable in an afternoon. -->

## What to steal

<!-- One or two sentences, or `_nothing_`. -->

## Same-cell comparison

<!-- One line per sibling card path given: compatible, or incompatible and
     what conflicts. `_no matrix_` or `_no siblings_` when that is the case.
     Read the sibling cards' Delta and Data-and-split sections; a difference
     in headline numbers on the same dataset and split is an incompatibility
     worth naming. -->

## References named

<!-- Every paper this reading names, one per line, title and year, untagged.
     /verify tags them on the card. Name only papers the paper itself cites in
     the passages read: a reference from memory is how a fabrication enters. -->

## What was read

<!-- The full-text path, its character count, and the pass: full, or 1 for a
     skim from the intro or the abstract. Then, in one line, what was NOT
     read: a paper read from an abstract alone says so here. -->
