# Diversity plan: <date>

<!-- One planning round, at research/ideas/plans/diversity-<date>.md. Four
     headings, fixed. Written by the diversity-planner agent, once per
     /research-bearings:ideas run.

     In `plans/`, not beside the idea pages: research/ideas/*.md is the pages
     directory, and check_ideas.py reads every file in it as an idea page. A
     plan sitting there fails eight heading rules on every run. Found by review
     2026-09-16, when it did.

     Nova: iteratively planning which fields to retrieve from, then generating
     again, produced 3.4 times more unique novel ideas than generating from the
     seed literature alone. This file is that plan.

     The planner names fields. It does not generate ideas — a planner that also
     proposed the ideas would be planning toward its own answer. -->

## What this set has in common

<!-- The honest reading, first, before anything is proposed. Two to four
     sentences: what every candidate in the set shares — the seed kind, the
     formulation, the benchmark, the assumption it is flipping, the field its
     nearest-existing paper came from.

     "All nine candidates flip a method bit, and eight of the nine put their
     query to the home vocabulary" is the finding this file exists for. If the
     set is genuinely varied, say that instead and propose fewer fields. -->

## Fields not drawn on

<!-- One line each: a field the set is not using, and what about the candidate
     set suggests it. This is the wider list, from which ## Proposed searches
     takes the ones worth a walk. -->

## Proposed searches

<!-- Three to six, ranked, best first. The main thread's cap is three per
     round, so the ranking is what it cuts on.

     ### <n>. <field>
     - Query: "<the query, in that field's own words>"
     - Blocked: <the home vocabulary terms that may not appear in the query>
     - Contributes: <one line — what this field would give the set that it
       lacks, in terms of the commonality named above>

     A field that shares the home citation graph is not a field: it is the home
     field with a wider collar, and the test is whether a paper there would
     plausibly cite the seeds. The script refuses a query using a blocked word,
     so a query written with one is a dispatch that will fail. -->

## Status

<!-- Date. Candidates read, and the seed kinds among them. Fields already
     searched, from what the dispatch supplied. Fields proposed. Anything the
     planner could not determine — a candidate with no nearest-existing line,
     a set too small to have a commonality, a home vocabulary it was not
     given. -->
