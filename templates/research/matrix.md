# Landscape matrix: <the question>

<!-- Written by the merger agent from research/landscape/sections/*.md and
     nothing else. Every paper here is a line copied from a section; every
     empty cell names the query that was run for it. The merger cannot add a
     claim that is not in a section. Six headings, fixed. -->

## Axes

<!-- Rows are formulations, columns are data regimes. One line per axis
     value, each with where it came from:
     - Formulation: <name> — from sections/formulations.md | surveys.md
     - Data regime: <name> — from sections/data-regimes.md | surveys.md -->

## Matrix

<!-- A markdown table, formulations down, regimes across. A filled cell says
     "N papers"; an empty cell says "query `<q>` returned zero rows" (or
     "returned N rows, 0 naming both", when the cell query returned rows that
     named neither axis value). The detail is under ## Cells. -->

## Cells

<!-- One block per cell, filled or empty:

     ### <formulation> × <data regime>
     - <paper line, copied from a section> — sections/<file>.md
     - <paper line> — sections/<file>.md

     or

     ### <formulation> × <data regime>
     - query `<q>` returned zero rows — sections/cells.md

     A paper goes in a cell only when a section line for it names both axis
     values, or sections/cells.md returned it for that cell's query. -->

## Contradictions

<!-- Where two sections disagree about one paper, or two papers in one cell
     make incompatible claims that the sections carry:
     - <title> — year 2019 in sections/a.md, 2021 in sections/b.md
     - <title> — verified in sections/a.md, candidate in sections/b.md
     These are idea seeds, not noise. If there are none, write: none found
     across N sections. -->

## Sources

<!-- Every section read, one per line, with its paper count. The merger read
     these and nothing else. -->

## Status

<!-- Date. Cells filled / empty / total. Contradictions count. Any section
     whose ## What was searched reported a degraded index, named. -->
