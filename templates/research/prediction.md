# Prediction: <title>

<!-- Written by the predictor agent, which has been given the intro file and
     nothing else. At research/papers/notes/<slug>.prediction.md.

     The predictor commits before it can be right by accident. That is the
     whole mechanism: where the prediction was wrong is what was not obvious,
     and it is only evidence if it was written down first. Five headings. -->

## Claim predicted from

<!-- The introduction's last paragraph, quoted. Mensh and Kording: that is
     where the central contribution is stated. Everything below is a
     prediction about how the rest of the paper cashes it out. -->

## Method

<!-- What this paper most likely does, in two or three sentences, then:
     Confidence: <1-5>

     Specific enough to be wrong. "Uses deep learning" is not a prediction. -->

## Main result

<!-- What the headline number most likely is, against what baseline, on what
     dataset, in two or three sentences, then:
     Confidence: <1-5>

     A number with a range is a prediction. "It improves" is not. -->

## Weakest point

<!-- Where this paper is most likely to be attacked: the comparison that will
     be unfair, the split that will leak, the ablation that will be missing,
     the claim the results will not support. Two or three sentences, then:
     Confidence: <1-5> -->

## What was read

<!-- The intro file path, its character count, and the split method from the
     fetch record: heading (naming it) or page-cut. If the split was a page
     cut, say so — it is the case where the predictor may have seen a little
     of the method, and the scorer should know before it scores. -->
